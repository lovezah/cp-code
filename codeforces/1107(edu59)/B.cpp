#include <bits/stdc++.h>

using namespace std;

void solve() {
    long long K;
    int X;
    cin >> K >> X;
    cout << (K-1) * 9 + X << '\n';
}

int main() {
    ios_base::sync_with_stdio(false); cin.tie(nullptr);
    int T; cin >> T;
    while (T--) {
        solve();
    }
    return 0;
}
